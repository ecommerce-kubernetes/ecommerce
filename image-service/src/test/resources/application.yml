spring:
  application:
    name:

upload-root:

gateway:
  domain:
  port:

eureka:
  client:
    enable: false
    fetch-registry: false
    register-with-eureka: false

swagger:
  allowed-origins:
    - "http://localhost:8000"