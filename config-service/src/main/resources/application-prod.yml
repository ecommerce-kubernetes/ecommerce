spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ecommerce-kubernetes/config.git
          username: ${GITHUB_USERNAME}
          password: ${GITHUB_TOKEN}
          default-label: main
          search-paths: /
      bus:
        enabled: true
        destination: springCloudBus # Kafka 토픽명
  kafka:
    bootstrap-servers: Kafka00Container:9092,Kafka01Container:9092,Kafka02Container:9092
